{
  "name": "@helpdesk/api",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "TICKETS_DELAY_MS=${TICKETS_DELAY_MS:-600} tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "evals": "tsx src/evals/suggestReply.eval.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@fastify/cors": "^9.0.0",
    "@helpdesk/core": "*",
    "fastify": "^4.28.0"
  },
  "devDependencies": {
    "tsx": "^4.16.0",
    "typescript": "^5.4.0"
  }
}

{
  "name": "orders-legacy",
  "version": "0.9.3",
  "private": true,
  "description": "Brownfield anchor project: an older order/inventory service. Used in Demos D15 and D17.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "API_TOKEN=dev-token node src/index.js",
    "smoke": "node -e \"require('./src/orders').listOrders((err, list) => { if (err || !Array.isArray(list) || list.length === 0) process.exit(1); console.log('orders smoke ok'); })\"",
    "test": "node --test"
  },
  "dependencies": {
    "body-parser": "1.18.3",
    "express": "4.16.4",
    "lodash": "4.17.11"
  }
}
